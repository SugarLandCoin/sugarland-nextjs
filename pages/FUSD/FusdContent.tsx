// import React, { useEffect, useState } from "react";
// import { ethers, BigNumber } from 'ethers';
// import { useTheme } from '@mui/material/styles';
import { Stack } from '@mui/material';
// import { Stack, Button, Grid, Typography, OutlinedInput, IconButton, Box } from '@mui/material';

// import { useYam, useTokenBalance, useUserApprovalOfContract } from '../../hooks';
// import { useYam } from '../../hooks';
// import { useWallet } from 'use-wallet';

export default function FusdContent() {
  // export default function FusdContent(props: any) {
  // const theme = useTheme();
  // const wallet = useWallet();
  // const yam = useYam();

  return (
    <Stack direction="column" spacing={3}>
      {/* <Typography variant="subtitle2" sx={{textAlign: 'center'}}>{ pageType }</Typography> */}

    </Stack>
  );
}